# basic-snake
snake in gwbasic

## 1. System Packages (APT)

Install required system dependencies:

```bash
sudo apt update
sudo apt install -y \
    pulseaudio-utils \
    xclip \
    xsel \
    wl-clipboard \
    libgtk-3-0
